- forbid trash if ownedBy any
- forbid trash if isColored color1
- forbid trash if inArea trash
- forbid pickUp if ownedBy 2
- forbid pickUp if isColored color2 and inArea trash
- forbid pickUp if ownedBy 3 and isColored color1
- forbid collect if not isColored color0
- forbid collect if inArea trash
- forbid collect if not ownedBy 1 and not ownedBy 3
